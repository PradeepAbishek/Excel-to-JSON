{
  "name": "excel-to-json",
  "version": "1.0.0",
  "description": "Converts the Excel file into JSON",
  "main": "src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "xls-to-json": "^0.5.4",
    "xlsx-to-json": "^0.3.0"
  }
}
